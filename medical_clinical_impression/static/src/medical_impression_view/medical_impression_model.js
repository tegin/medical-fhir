odoo.define("medical_clinical_impression.MedicalImpressionModel", function (require) {
    "use strict";

    var BasicModel = require("web.BasicModel");

    const MedicalImpressionModel = BasicModel.extend({});

    return MedicalImpressionModel;
});
