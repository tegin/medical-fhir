This module allows to use observations inside impressions. It is quite useful in some environments, where we want to set data like:

- Nursery wants to introduce temperature
- Doctor wants to introduce parameters on the clincial history
